@model YothingoSprint1.Models.Room



    @{
        ViewBag.Title = "Delete Status";
    }
    
        <h2>Delete Status</h2>

        <h3>Are you sure you want to delete this Status?</h3>
        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.FridgeDescription)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.FridgeDescription)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">

                    @Html.ActionLink("Back", "Index", new { id = Model.ID }, new { @class = "btn btn-info" })|
                    <input type="submit" value="Delete" class="btn btn-danger" />
                </div>
            }
        </div>
    </div>

    </div>